# 可选：宿主机读取的构建配置（避免依赖 jq 解析 JSON）。
# 若存在，将优先于 config.json 被 build.sh 读取。

# 基础镜像配置
BASE_IMAGE_NAME=ssl-manager-build-base
BASE_IMAGE_TAG=ubuntu-24.04
REBUILD_DAYS=30

# 构建镜像配置
BUILD_IMAGE_NAME=ssl-manager-build
BUILD_IMAGE_TAG=latest
