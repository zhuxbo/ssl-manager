<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 上级 API 配置
    |--------------------------------------------------------------------------
    | 配置上级 Gateway 或 Manager 的 REST API 地址和密钥
    */
    'api' => [
        'base_url' => env('ACME_GATEWAY_URL'),
        'api_key' => env('ACME_GATEWAY_KEY'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Nonce 配置
    |--------------------------------------------------------------------------
    | Nonce 用于防止重放攻击
    */
    'nonce' => [
        'driver' => 'redis',
        'ttl' => 3600, // 秒
    ],

    /*
    |--------------------------------------------------------------------------
    | 订单配置
    |--------------------------------------------------------------------------
    */
    'order' => [
        'expires_in' => 86400 * 7, // 订单有效期（秒）
        'default_product_id' => env('ACME_DEFAULT_PRODUCT_ID'),
    ],
];
